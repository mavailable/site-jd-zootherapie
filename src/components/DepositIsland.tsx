// AUTO-GENERATED par @marc/cms-engine — ne pas editer. Regenerer : npx cms-engine-scaffold
import { DepositForm } from '@marc/cms-engine/modules/vocaux-deposit';
import type { CmsConfig } from '@marc/cms-engine';
export default function DepositIsland({ config }: { config: CmsConfig }) {
  return <DepositForm config={config} />;
}
